DROP TABLE HOJA;
    CREATE TABLE HOJA(ID INTEGER, EDAD INTEGER, NOMBRE VARCHAR(120));
       DECLARE
         BEGIN
      FOR I IN 1..300 LOOP
      DBMS_OUTPUT.PUT_LINE(I||'PRIMER CICLO');
      INSERT INTO HOJA VALUES(I,30, 'ANA');

   END LOOP;
END;

SELECT * FROM HOJA;
